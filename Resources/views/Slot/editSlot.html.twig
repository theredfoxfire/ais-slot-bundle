{% block content %}
    <h1>Edit Slot Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_update_slot', {'id': slot.id}) }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
